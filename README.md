# Interview Questions - Blog Task

 1. What are some differences between interfaces and types in TypeScript?
    ## Interface Declare:
   ```ts  
 interface Person {
  name: string;
  age: number;
}
```
# Type alias

```ts  
type Person = {
  name: string;
  age: number;
};
```


 1. 